spring:
  neo4j:
    database: careergraph
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: Congquy2004@
    pool:
      max-connection-pool-size: 16
      idle-time-before-connection-test: 10s
      max-connection-lifetime: 1h

  datasource:
    url: jdbc:mysql://localhost:3306/careergraph?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  redis:
    host: localhost
    port: 6379
    timeout: 2000ms
    database: 0
    password:
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
      shutdown-timeout: 100ms